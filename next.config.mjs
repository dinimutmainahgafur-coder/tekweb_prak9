/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  basePath: '/tekweb_prak9',
  assetPrefix: '/tekweb_prak9/',

  images: {
    unoptimized: true,
  },
};

export default nextConfig;
