/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",              
  basePath: "/tekweb_prak9",      
  images: {
    unoptimized: true,           
  },
};

module.exports = nextConfig;
